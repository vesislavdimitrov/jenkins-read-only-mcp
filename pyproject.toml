[project]
name = "jenkins-mcp"
version = "1.0.0"
description = "Minimalistic read-only Jenkins MCP server"
requires-python = ">=3.12"
dependencies = [
    "mcp[cli]>=1.0.0",
    "httpx>=0.27",
]

[project.scripts]
jenkins-mcp = "server:mcp.run"